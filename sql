CREATE DATABASE organic_farm;

USE organic_farm;

CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    address TEXT,
    total_amount DECIMAL(10, 2)
);
--  // sql file 










